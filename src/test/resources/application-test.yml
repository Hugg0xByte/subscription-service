spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/subscription_db_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    enabled: false
