/**
 * Domain layer: entities, value objects, enums, and domain events.
 * This layer has ZERO framework dependencies (no Spring, no JPA, no infrastructure libraries).
 */
package com.globo.subscription.domain;
