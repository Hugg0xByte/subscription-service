/**
 * Adapter layer: inbound adapters (REST controllers, schedulers) and
 * outbound adapters (persistence, payment, cache, event publisher, lock manager).
 */
package com.globo.subscription.adapter;
